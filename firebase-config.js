// Firebase web config for SUBBU. These values identify the project and are safe to publish:
// access is controlled by firestore.rules (each signed-in user reads and writes only their own data).
// Set to null to run without sign-in (data stays in the browser).
window.SUBBU_FIREBASE = null;
